"""
API 공통 의존성.
- DB 세션, 현재 job 조회 등 (추후 구현).
"""
# from fastapi import Depends
# from sqlalchemy.orm import Session
# from app.core.database import get_db

# def get_current_job(job_id: str, db: Session = Depends(get_db)):
#     ...
